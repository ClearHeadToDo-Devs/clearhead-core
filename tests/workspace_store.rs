//! Integration tests for the workspace store (load/save pipeline).
//!
//! Sources are split across `tests/workspace/` submodules; this file is the
//! single test-binary entry point (Cargo compiles only top-level `tests/*.rs`).

mod workspace;
