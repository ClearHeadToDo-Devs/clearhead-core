
im trying to figure out the proper boundary between `ontology` and `tree-sitter-actions`

in particular, i want to automatically generate a new parser based on the ontology/SHACL shapes we define but im unsure how to go about it.

i made a generator that create a JSON schema and im thinking we post that on a website we own while still working through our stuff but im unsure because it feels like there should be an easier way to generate a treesitter parser from our work here
